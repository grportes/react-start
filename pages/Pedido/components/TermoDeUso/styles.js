import {makeStyles} from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    container: {
      padding: '20px'
    },
    titulo: {
        textAlign: "center",
        color: '#000',
        fontWeight: 900,
        fontSize: '18px',
        marginBottom: '10px'
    },
    subTitulo: {
        color: '#000',
        fontWeight: 900,
    },
    topico: {
        color: '#000',
        fontWeight: 900,
    },
    topicoCorpo: {
        padding: '10px',
    },
    subTopico: {
        color: '#000',
        fontWeight: 900,
    }
}));